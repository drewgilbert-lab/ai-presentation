{
  "buildCommand": "npm run build",
  "outputDirectory": "dist",
  "rewrites": [
    { "source": "/:slug/assets/:path*", "destination": "/:slug/assets/:path*" },
    { "source": "/:slug/:path*", "destination": "/:slug/index.html" }
  ]
}
